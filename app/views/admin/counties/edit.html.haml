%h1 Update County

= simple_form_for(@county, url: admin_county_path(@county), method: :put) do |form|
  = form.input :name
  = form.input :state
  = form.input :latitude
  = form.input :longitude
  = form.button :submit, class: 'btn-primary'
