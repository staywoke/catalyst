%h1 Edit County

= simple_form_for(@county, url: admin_county_path(@county), method: :put, html: {class: 'form-horizontal'}) do |form|
  = form.input :name
  = form.input :state
  = form.input :latitude
  = form.input :longitude
  = form.button :submit, class: 'btn btn-primary'
