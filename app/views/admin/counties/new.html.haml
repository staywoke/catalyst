%h1 New County

= simple_form_for(@county, url: admin_counties_path, method: :post, html: {class: 'form-horizontal'}) do |form|
  = form.input :name
  = form.input :state
  = form.input :latitude
  = form.input :longitude
  = form.button :submit
