%nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      %button.navbar-toggle.collapsed{"aria-expanded" => "false", "data-target" => "#collapser", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => root_path}
        %img{alt: 'StayWoke', src: asset_path('logo.png')}
    #collapser.collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-right
        %li.active
          %a{:href => dashboard_path}
            Actions
            %span.sr-only (current)
          - if current_user
            %li= link_to 'My Account', account_path
          - if current_user.try(:admin?)
            %li.dropdown
              %a.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :href => "#", :role => "button"}
                Admin
                %span.caret
              %ul.dropdown-menu
                %li= link_to 'Cities', admin_cities_path
                %li= link_to 'Counties', admin_counties_path
                %li= link_to 'Domains', admin_domains_path
                %li= link_to 'Projects', admin_projects_path
          - if current_user
            %li= link_to 'Logout', destroy_user_session_path, method: :delete
