= simple_form_for(@user, url: account_path, method: :put, html: {class: 'form-horizontal'}) do |form|
  = form.input :first_name
  = form.input :last_name
  = form.input :email
  = form.input :zip_code
  = form.input :old_password
  = form.input :new_password
  = form.button :submit
